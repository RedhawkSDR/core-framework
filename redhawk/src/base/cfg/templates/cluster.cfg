[CLUSTER]
name = {{cluster}}

[EksKube]
registry = {{registry}}
tag = latest
dockerconfigjson = {{dockerjsonconfig}}

[DockerSwarm]
registry = {{registry}}
tag = latest

key = {{ssh_key}}
user = {{server_user}}
ip = {{server_ip}}
docker_login_cmd = "docker login"

[Docker]
local_dir={{docker_dir}}
mount_dir={{mount_dir}}
